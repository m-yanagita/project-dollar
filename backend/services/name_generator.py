def generate_names(industry, keyword):

    names = [
        f"{keyword} Labs",
        f"{industry} Nova",
        f"{keyword} Future",
        f"Next {industry}",
        f"{keyword} Hub"
    ]

    return names